# Implementation Plan: Full Lichess Filters

## 1. Context & Goal
The user requires granular control over Lichess statistics. We will expand the settings panel in the Dashboard to include **all** available Lichess time controls (including UltraBullet and Correspondence) and **all** rating brackets (starting from 400).

## 2. Memory Update
Updating `src/dashboard/App.tsx` to include the complete set of Lichess API filter options for `speeds` and `ratings`.

## 3. Step-by-Step Instructions

### Step 1: Expand Dashboard Settings
**Action:** Modify `src/dashboard/App.tsx`
**Description:**
- **Update Time Controls:**
    - Change list to: `['ultraBullet', 'bullet', 'blitz', 'rapid', 'classical', 'correspondence']`.
    - **UI Polish:** Add a helper to format `ultraBullet` as "Ultra Bullet" (insert space before capital letters) for better readability.
- **Update Rating Ranges:**
    - Change list to: `[400, 1000, 1200, 1400, 1600, 1800, 2000, 2200, 2500]`.

## 4. Verification
1.  Run `npm run build`.
2.  Reload extension.
3.  Open Dashboard -> Settings.
4.  Verify all checkboxes appear (UltraBullet to Correspondence; 400 to 2500).
5.  Select "Correspondence" only, refresh stats, and verify the numbers change (likely lower for opening theory).
